# פרומפט הרצה אוטונומית — מימוש DESIGN-UPGRADE המלא

תממש את תוכנית שדרוג העיצוב של האתר הזה מהתחלה ועד הסוף, באופן אוטונומי לחלוטין. אני לא זמין — אל תשאל אותי כלום; כל החלטה פתוחה תוכרע לפי המסמכים, תתועד, ותופיע בדוח הסיום. תרוץ כמה שצריך (גם יום שלם) עד שהכל מוכן, בדוק ומאומת.

## שלב 0 — קרא לפני שאתה נוגע בקוד (לפי הסדר)
1. `DESIGN-UPGRADE.rtl.md` — התוכנית. זה המפרט המחייב, כולל סעיף 7 (תקציב ביצועים) שהוא קו אדום.
2. `CLAUDE.md` — במיוחד בלוק "STATUS UPDATE — July 2026" (אין CMS, הפלטה ממופה מחדש, orange=#7DD3FC).
3. `.context/HANDOFF.md` — מה כבר נעשה ולמה.
4. סרוק את `src/` בעצמך לפני כל שינוי בקובץ — אל תסמוך על תיעוד ישן.

## חוזה האוטונומיה
- עבודה על branch חדש: `design/wave-lab` מעל `feature/production-polish`. קומיט קטן וסמנטי אחרי כל סעיף שעובר אימות. לעולם לא לעבוד על main.
- נתקעת פעמיים על אותה בעיה? עצור וחפש ברשת את הפתרון לפני ניסיון שלישי.
- החלטה עיצובית פתוחה? בחר את האופציה השמרנית יותר, תעד ב-`DECISIONS.rtl.md` והמשך.
- אסור לעצור באמצע בגלל "צריך אישור" — יש מדיניות fallback לכל חסם (ראה בהמשך).

## היקף
- **Phase 1 + 2 + 3 במלואם** לפי סדר התוכנית.
- **Phase 4**: רק MagneticButton כבר כלול ב-3; סמן מותאם + View Transitions לבלוג — רק אם כל השערים ירוקים ונשאר לך כוח; פליגראונד ו-Lenis — לא בריצה הזו.
- פריטים שחסומים על נכסים/החלטות שלי — מדיניות מחייבת:
  - **פונטים (סעיף 8.2):** אל תקנה ואל תחליף פונט. ממשיכים עם הפונטים הקיימים; את "פונט המפרט" למספרים תממש עם `font-variant-numeric: tabular-nums` + מונו מערכתי בעברית fallback (הפתרון החינמי מהתוכנית). רישיון EFT נשאר פתוח בדוח.
  - **פס ההוכחות (2.3):** להשתמש אך ורק במספרים הניתנים לאימות מתוך הריפו עצמו: ציון Lighthouse שתמדוד בעצמך בסוף הריצה, זמן LCP שנמדד, מספר הדפים באתר. שום "מספר לקוחות/שנים" מומצא. את שאר הסלוטים לבנות מוכנים-אך-מוסתרים (`data-pending` + הערה).
  - **סליידר לפני/אחרי (3.1):** לבנות את הקומפוננטה במלואה עם שתי תמונות דמו שתייצר בעצמך (צילום מסך של האתר הנוכחי מול גרסה "מיושנת" מלאכותית שתרנדר), עם תווית ברורה "הדגמה". בלי שקרים על לקוחות.
  - **קופי עברי חדש:** מותר מיקרו-קופי פונקציונלי מינימלי בלבד (תוויות, מד הטעינה, כותרת פס ההוכחות). כל מחרוזת חדשה נרשמת ב"טבלת קופי לאישור" בדוח הסיום כדי שאוכל להחליף בקלות. אין סיסמאות שיווקיות מומצאות.

## מוקשים ידועים (למדנו בדם — אל תחזור עליהם)
- `vite.config.ts`: **אסור** להחזיר sanity/`@sanity/*` ל-manualChunks — זה גרר 1.26MB לכל מבקר.
- framer: **רק `m` components** (LazyMotion strict). לעולם לא `motion`. אין `layoutId`/`drag` (דורשים domMax).
- עברית: פיצול כותרות **לפי מילים בלבד** — פיצול אותיות שובר גליפים. אין letter-spacing ואין italic בעברית.
- Chrome headless מדמה `prefers-reduced-motion: reduce` — בצילומי אימות של אנימציות חובה `page.emulateMediaFeatures([{name:'prefers-reduced-motion', value:'no-preference'}])`, אחרת תסיק בטעות שהאנימציות שבורות.
- הטפסים עובדים ב-WhatsApp handoff כשאין endpoint — אל תשבור את הזרימה הזו.
- פורטים 4173/4174 לפעמים תפוסים — vite preview יעלה על אחר; קרא את הפורט מהפלט, אל תניח.
- כל לופ אינסופי חדש: gate על useReducedMotion + השהיה מחוץ למסך (יש דפוס מוכן ב-LottieIcon.tsx).

## שערי איכות — אחרי כל Phase (לא ממשיכים בלעדיהם)
1. `npx tsc -b` נקי + `npx eslint src` בלי שגיאות חדשות.
2. `npm run build` מלא (כולל prerender של 7+ דפים) עובר.
3. Lighthouse על preview: **דסקטופ ≥ 95, מובייל ≥ 70**. ירידה מתחת — revert הסעיף האחרון או תקן לפני המשך. שמור את דוחות ה-JSON.
4. צילומי Puppeteer ב-320/390/768/1440 של כל הסקשנים + `/blog` + `/accessibility` + 404: אפס גלילה אופקית, אפס שגיאות קונסול, אפס בקשות כושלות. **הסתכל על הצילומים בעיניים** — דברים שנראים שבורים לא עוברים כי "הקוד קומפל".
5. רגרסיית אנימציות (עם no-preference): typewriter, וידאו הירו עולה בדסקטופ, Lotties מתנגנים במסך ונעצרים מחוצה לו, אקורדיון FAQ, תפריט מובייל, וכל האפקטים החדשים.
6. בדיקת reduced-motion: הכל קריא וסטטי.

## סיום — מה אתה חייב להציג לי
1. `npm run build` אחרון ירוק + כל השערים ירוקים.
2. הרם `npm run preview -- --host` והשאר אותו רץ. הצג לי **בשורה הראשונה של ההודעה האחרונה**: כתובת Local + כתובת Network (לטלפון), עם הפורט האמיתי מהפלט.
3. דוח סיום בעברית: מה מומש מכל phase, ציוני Lighthouse לפני/אחרי, טבלת הקופי לאישור, החלטות שקיבלת (`DECISIONS.rtl.md`), מה נשאר פתוח (רישיון פונטים, מספרים אמיתיים, נכסי לקוח), ונתיבי צילומי לפני/אחרי.
4. עדכן את `.context/HANDOFF.md`.
5. אל תעשה push ואל תמחק כלום מחוץ לריפו.
