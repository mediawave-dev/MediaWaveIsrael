# 🌊 MediaWave — תוכנית שדרוג מראה: "האתר שהוא ההוכחה"

> **מסמך מימוש** — נבנה על בסיס אודיט מלא של האתר (יולי 2026), מחקר רשת על אתרי סוכנויות עטורי פרסים 2025-2026, טיפוגרפיה עברית פרימיום, ודפוסי אמון. כל פריט כולל: קבצים, גישת מימוש, עלות ביצועים, והערכת מאמץ.
>
> **קו אדום:** Lighthouse דסקטופ ≥ 95, מובייל lab ≥ 70 אחרי כל שלב. אנימציות קיימות לא נשברות. אפס טקסט עברי מומצא — כל קופי חדש מסומן `[קופי: נתי]` וממתין לאישור.

---

## 1. הכיוון המחויב (לא "נקי ומודרני" — משהו ספציפי)

### "מעבדת הגלים" — Dark-tech craft עם חתימת גל

שלושה עמודי תווך שהופכים את האתר לבלתי נשכח:

1. **הגל כשפת מותג** — אנחנו MediaWave והלוגו הוא גלים, אבל באתר אין אף גל. זה החור הכי גדול וההזדמנות הכי גדולה: מערכת גלים חתימתית — מפרידי סקשן אורגניים, גל מצויר-נמשך (draw-on) בהירו, גל התקדמות. אף תבנית לא תיראה ככה.
2. **האתר הוא ההוכחה (meta-proof)** — מד טעינה שנמדד אצל המבקר עצמו (בלתי ניתן לזיוף), סליידר לפני/אחרי עם דלתא Lighthouse אמיתית, מספרים כנים. טענה → אימות חי → הזמנה לבדוק בעצמך.
3. **טיפוגרפיה עברית קינטית** — כותרת הירו עצומה עם reveal מדורג לפי מילים (לא אותיות! שובר עיצוב אותיות עברי), קטבי משקל קיצוניים, מונו-עברי/טבולרי למספרים ותוויות.

**מבחן ה"בלתי נשכח":** מבקר סוגר את הטאב וזוכר — "האתר עם הגלים שנמשכים, שהראה לי כמה מהר הוא נטען אצלי."

### מה נשאר (לא נוגעים)
- הפלטה: נייבי כהה + תכלת חשמלית `#7DD3FC` — **בדיוק** על טרנד ה-retro-futurism הישראלי 2026 (אומת במחקר: gwspirits, One Zero). לא מחליפים.
- כל אנימציות ה-Lottie, ה-typewriter, ה-glassmorphism בכרטיסים — נשארים.
- התשתית: `m` components בלבד, LazyMotion strict, `useReducedMotion` בכל לופ.

---

## 2. חוקי אנטי-AI-slop (מחייבים בכל שינוי)

- ❌ אין gradient סגול, אין 3 כרטיסים גנריים בשורה בלי טוויסט, אין אימוג'י כאייקונים
- ❌ אין letter-spacing בעברית (סבילות עברית לריווח אותיות נמוכה — נראה חובבני)
- ❌ אין איטליק/הטיה מלאכותית בעברית
- ✅ קונטרסט משקל קיצוני: Thin מול Black, לדלג על המשקלים האמצעיים
- ✅ קפיצות גודל פי 3+ בין רמות היררכיה (עברית צריכה ~10% יותר מלטינית באותה רמה)
- ✅ easing חתימה אחד לכל האתר: `cubic-bezier(0.16, 1, 0.3, 1)` (כבר קיים כ-`--ease-out-expo`) — לאכוף בכל framer transition דרך קובץ אחד
- ✅ מהלך חתימה **אחד** לסקשן. איפוק = יוקרה (הדוקטרינה שחזרה בכל המקורות)
- ✅ גריין עדין על רקעים — הסיגנל הזול ביותר נגד "מראה AI שטוח"

---

## 3. שפת מותג — טוקנים חדשים

### 3.1 טיפוגרפיה
| תפקיד | פונט | הערות |
|---|---|---|
| כותרות ענק (הירו, סקשנים) | EFT Betaamango (קיים) | ⚠️ **חובה לוודא רישיון web** — לא נמצא באף קטלוג ישראלי (פונטימונים/אאא/פונטף). אם אין רישיון: להחליף ב-**Axioma** (פונטימונים, ~245₪/משקל) או **Almoni Tzar** לקונדנסד דרמטי |
| טקסט רץ | EFT OffSet (קיים) | אותה בדיקת רישיון |
| **חדש: תוויות/מספרים/קוד** | מונו עם תמיכה עברית או טבולרי-לטיני | טרנד "code nostalgia" 2026; המספרים (`0.9s`, `99/100`, `01–04`) יקבלו זהות "גיליון מפרט". אופציה חינמית: `font-variant-numeric: tabular-nums` על ה-Latin הקיים + class `.font-mono-spec` |

**דפוס חדש:** מילת מפתח אחת בכל כותרת סקשן צבועה `#7DD3FC` (מחליף את מה שאותיות רישיות היו עושות באנגלית — כך עושים היררכיה עברית ב-2026).

### 3.2 טוקני CSS חדשים (`src/styles/index.css`)
```css
@theme {
  --font-spec: 'IBM Plex Mono', 'Courier New', monospace; /* תוויות/מספרים */
  --text-giant: clamp(4rem, 12vw, 11rem);                 /* מילת הצהרה */
  --ease-brand: cubic-bezier(0.16, 1, 0.3, 1);
  --grain-opacity: 0.05;
}
```

### 3.3 גריין (Quick-win מס' 1)
שכבת noise גלובלית — **PNG מתפזר 128px ב-base64** (לא feTurbulence חי — הורס paint במובייל):
```css
.grain-overlay {
  position: fixed; inset: 0; pointer-events: none; z-index: 1;
  background-image: url(data:image/png;base64,...); /* טקסטורה אפויה */
  opacity: var(--grain-opacity); mix-blend-mode: overlay;
}
```
- קובץ: `src/components/ui/GrainOverlay.tsx` (חדש) נטען ב-`Layout.tsx`
- עלות: אפס (שכבת קומפוזיטור סטטית) · מאמץ: שעה

---

## 4. מהלכי WOW לפי סקשן

### 4.1 הירו — "הגל הראשון" ⭐ המהלך המרכזי
**היום:** תמונת לפטופ + typewriter. עובד, אבל שום דבר שלא ראינו.
**היעד:**
1. **Reveal מדורג לפי מילים** של הכותרת: כל מילה בתוך `span` עם `overflow:hidden`, נכנסת `y: 110% → 0` עם `staggerChildren: 0.08` וה-easing החתימתי. נכנסות **מימין** (RTL).
   - קובץ: `src/components/sections/Hero.tsx` + קומפוננטה חדשה `src/components/ui/StaggeredWords.tsx` (~40 שורות, שימוש חוזר)
   - ⚠️ פיצול לפי **מילים בלבד** — פיצול אותיות שובר עיצוב גליפים עברי
2. **גל SVG חתימתי נמשך** מתחת לכותרת: path בסגנון גלי הלוגו, `pathLength: 1`, אנימציית `stroke-dashoffset 1→0` על scroll הראשון — מחליף את הקו הישר הסטטי מתחת ל"המומחיות שלנו"
   - עלות: C-tier אבל SVG קטן = זניח (עד 3 draw-ons בו-זמנית באתר)
3. **מד טעינה אישי** (ראו 5.1) בפינת ההירו
- typewriter נשאר; וידאו הרקע העצל נשאר כמו שהוא
- מאמץ: יום · Lighthouse: אפס פגיעה (transform/opacity + one-shot)

### 4.2 פס לוגואים/שירותים נע (Marquee) — Quick-win מס' 2
פס אינסופי בין הירו ל-WhyUs: שמות שירותים או לוגואי טכנולוגיות (React, Vite, Cloudflare…) — CSS בלבד:
```css
@keyframes marquee-rtl { to { transform: translateX(50%); } } /* RTL: כיוון הפוך */
```
- track משוכפל עם `aria-hidden="true"`, `animation-play-state: paused` ב-hover, סטטי תחת reduced-motion
- קובץ חדש: `src/components/ui/Marquee.tsx` · מאמץ: 2-3 שעות · עלות: אפס (transform יחיד)

### 4.3 סקשן כהה חדש — "פס ההוכחות" ⭐ עוגן האמון
סקשן **אחד** כהה (נייבי `#1E293B`, לא שחור) בין Services ל-HowWeWork — הניגוד עצמו הוא כלי העיצוב (אתר בהיר + סקשן כהה אחד = מהלך בטוח ואלגנטי, אומת ב-One Zero/gwspirits):
- **מספרים חיים בקאונט-אפ**: פרויקטים, שנות ניסיון, ציון Lighthouse ממוצע — `useInView` + motion value → `textContent` (בלי setState לכל פריים!)
  - `[קופי: נתי — המספרים חייבים להיות אמיתיים וקטנים-וכנים; "12 אתרים, כולם מעל 95" מנצח "מאות לקוחות"]`
- מספרים בפונט המפרט (`--font-spec`), תוויות בעברית
- גריין + mesh עדין של תכלת על הרקע הכהה
- מפרידי גל אורגניים בכניסה וביציאה (4.6)
- קובץ חדש: `src/components/sections/ProofBand.tsx` + הוספה ל-`App.tsx` (lazy) · מאמץ: יום

### 4.4 סליידר לפני/אחרי — ההוכחה האינטראקטיבית ⭐
בתוך פס ההוכחות או Portfolio עתידי: השוואת גרירה של פרויקט לקוח (yaelevy?) לפני/אחרי + **דלתא Lighthouse אמיתית** ("46 → 98"):
- מימוש עצמי (מתאים לאתוס "בונים בעצמנו"): שתי תמונות מוערמות, העליונה עם `clip-path: inset()`, גרירה מעדכנת CSS var דרך `useMotionValue` (אפס re-renders) — clip-path = S-tier קומפוזיטור
- RTL: הידית נכנסת מימין, גרירה הפוכה
- דורש מנתי: צילומי לפני/אחרי אמיתיים + אישור הלקוח. **אסור** להציג מוקאפ כלקוח אמיתי
- קובץ חדש: `src/components/ui/BeforeAfterSlider.tsx` (~80 שורות, אפס תלויות) · מאמץ: יום

### 4.5 Services — ריפרש כרטיסים
- ה-glassmorphism נשאר (טרנד ישראלי 2026 מאומת) — מוסיפים **חשיפת hover עם clip-path** (wipe מימין) על שכבת גוון, ו-**מספור מפרט** `01·` `02·` בפונט המפרט (דפוס One Zero)
- צל hover: להחליף אנימציית `boxShadow` (יקר, CPU) ב-`::after` עם הצל מוכן + אנימציית `opacity` (S-tier) — גם שיפור ביצועים
- קבצים: `Services.tsx`, `index.css` · מאמץ: חצי יום

### 4.6 מפרידי גל אורגניים בין כל הסקשנים ⭐ החתימה
לא גלי getwaves גנריים — גלים **א-סימטריים בהשראת הלוגו** (Haikei/יד חופשית), `preserveAspectRatio="none"`, fill = צבע הסקשן הבא:
- בין: Hero→Marquee→WhyUs→Services→ProofBand→HowWeWork→FAQ→Contact→Footer
- שניים מהם עם שכבת גל שנייה בדריפט איטי (2 אנימציות transform זולות) — עומק
- קובץ חדש: `src/components/ui/WaveDivider.tsx` (prop: `variant`, `flip`, `fill`) · מאמץ: חצי יום + חצי יום עיצוב ה-paths

### 4.7 HowWeWork — קו התהליך כגל
הקווים המקווקווים בין השלבים הופכים ל-path גלי אחד שנמשך לרוחב כל הארבעה ב-scroll (draw-on אחד במקום 3 קווים) — ממשיך את שפת הגל · מאמץ: חצי יום

### 4.8 כפתורים מגנטיים (CTA ראשיים בלבד)
"בואו נדבר" (הדר) + CTA הירו + "שליחת הודעה": הזזה 4-8px לכיוון הסמן עם spring, **דסקטופ בלבד** (`pointer: fine`):
- קובץ חדש: `src/components/ui/MagneticButton.tsx` (useMotionValue+useSpring, ~40 שורות) · מאמץ: חצי יום · עלות מובייל: אפס (כבוי)

### 4.9 מילת ענק אחת (סקשן FAQ או לפני Contact)
מילה עברית אחת פורשת את כל רוחב המסך (`--text-giant`, עד 12vw), outline (`-webkit-text-stroke`) שמתמלא ב-hover — הצהרת ביטחון טיפוגרפית · `[קופי: נתי — מילה אחת]` · CSS בלבד · מאמץ: שעתיים

### 4.10 סמן מותאם (שלב אחרון בלבד!)
נקודה + טבעת בפיגור spring, גדלה עם תווית ("צפו") מעל הסליידר והפורטפוליו. `pointer: fine` בלבד, הסמן המקורי נשאר. **מיישמים רק אחרי שכל השאר מושלם** — סמן מותאם על אתר לא-מלוטש = גימיק · מאמץ: יום

---

## 5. אלמנטי "האתר הוא ההוכחה"

### 5.1 מד הטעינה האישי ⭐ (בלתי ניתן לזיוף)
PerformanceObserver מודד את ה-LCP **של הביקור הזה** ומציג אחרי שה-LCP כבר קרה (לא פוגע בו):
```
"העמוד הזה נטען אצלך ב-0.8 שניות" [קופי: נתי]
```
~15 שורות, אפס תלויות. מוצג בהירו (מופיע בעדינות אחרי הטעינה) או בפוטר.
- קובץ חדש: `src/components/ui/LoadTimeBadge.tsx` · מאמץ: 2-3 שעות

### 5.2 קישור "בדקו בעצמכם"
ליד המד: קישור ישיר ל-`pagespeed.web.dev/?url=mediawaveisrael.com` — הופך טענה להזמנה לאימות. הצורה החזקה ביותר של אמון (וגם מחייבת אותנו להישאר מהירים).

### 5.3 כללי כנות (קו אדום)
- כל מספר חייב להיות ניתן לשחזור; מספר קטן וכן > מספר גדול ומעורפל
- מחקר: זיוף הוכחה חברתית הוא ה-dark-pattern הנפוץ ביותר — גילוי = אובדן אמון לצמיתות
- Testimonials נשאר כבוי עד שיש המלצות אמיתיות (כבר מיושם כך)
- אימות צד-שלישי (ביקורות גוגל) > ציטוטים עצמיים — לשקול הטמעת Google Reviews כשיהיו

### 5.4 עתידי (Phase 4+): מיקרו-פליגראונד
"שנו את זה, תראו מה קורה" — 3 סליידרים (צבע/רדיוס/spring) שמשנים כרטיס דמו חי + בלוק `<code>` שמדפיס את ה-CSS שנוצר. אפס תלויות (~5KB). לא Sandpack (מאות KB — הורג את המובייל).

---

## 6. שלבי מימוש

### Phase 1 — יסודות + Quick Wins (יום-יומיים) → קומיט לכל סעיף
| # | מה | עלות Lighthouse |
|---|---|---|
| 1.1 | טוקנים חדשים + easing חתימה מרוכז (`src/config/motion.ts` מיוצא לכל framer transition) | 0 |
| 1.2 | GrainOverlay גלובלי | 0 |
| 1.3 | WaveDivider — 3 וריאנטים, פריסה בין כל הסקשנים | 0 |
| 1.4 | Marquee טכנולוגיות/שירותים | 0 |
| 1.5 | תיקון צל-hover בכרטיסים ל-pseudo+opacity | שיפור! |

### Phase 2 — ההירו והאמון (2-3 ימים)
| # | מה |
|---|---|
| 2.1 | StaggeredWords בהירו + גל draw-on חתימתי |
| 2.2 | LoadTimeBadge + קישור "בדקו בעצמכם" |
| 2.3 | ProofBand — הסקשן הכהה עם קאונט-אפ (ממתין למספרים מנתי) |
| 2.4 | מספור מפרט + מילות מפתח צבועות בכותרות סקשן |

### Phase 3 — אינטראקציה (2-3 ימים)
| # | מה |
|---|---|
| 3.1 | BeforeAfterSlider (ממתין לנכסי לקוח מנתי) |
| 3.2 | Services hover wipe + HowWeWork קו גלי נמשך |
| 3.3 | MagneticButton על 3 CTA |
| 3.4 | מילת הענק |

### Phase 4 — ליטוש עילית (אופציונלי, אחרי אישור נתי)
סמן מותאם · View Transitions לבלוג (feature-detect, <300ms) · מיקרו-פליגראונד · Lenis דסקטופ-בלבד (לשקול — 3KB, מגע נשאר נייטיב)

---

## 7. תקציב ביצועים וכללי מוטור (לא עוברים)

1. **מותר לאנמט רק:** `transform`, `opacity`, `clip-path` (+`filter` על אלמנטים קטנים, רדיוס ≤8px, לעולם לא full-viewport)
2. **אסור לאנמט:** `box-shadow`, `background-position`, `width/height/top/left`, CSS vars מורשים על עצים גדולים
3. scroll-linked רק דרך `useScroll` → motion values ישירות ל-`style` (משתמש ב-ScrollTimeline API כשקיים) — **אף פעם** motion value → setState
4. reveals דקורטיביים: עדיפות ל-CSS `animation-timeline: view()` בתוך `@supports` (אפס main-thread; פיירפוקס יציב עדיין מאחורי דגל — ה-fallback הוא המצב הסופי)
5. כל לופ אינסופי חדש: gate על `useReducedMotion` + השהיה מחוץ למסך (הדפוס כבר קיים ב-LottieIcon)
6. תוספת JS מותרת ל-Phase 1-3 כולו: **≤ 15KB gzip** (הכל framer שכבר קיים + קוד עצמי)
7. אחרי כל Phase: `npm run build` + סקריפט הצילומים (320/390/768/1440) + Lighthouse — דסקטופ ≥95, מובייל ≥70. נפילה = revert או תיקון לפני המשך
8. אימות ויזואלי: `/frontend-super` → `/evaluate-design` אחרי כל סקשן; ציון < 70 → `/progressive-refine`

---

## 8. מה נדרש מנתי לפני/במהלך המימוש

| # | החלטה/נכס | חוסם את |
|---|---|---|
| 1 | ✅/❌ אישור הכיוון "מעבדת הגלים" | הכל |
| 2 | בדיקת רישיון web לפונטים EFT Betaamango/OffSet (לא נמצאו באף קטלוג!) — או תקציב ~500₪ לפונט תצוגה מפונטימונים | Phase 2 |
| 3 | מספרים אמיתיים לפס ההוכחות (כמה אתרים, שנים, ציונים) | 2.3 |
| 4 | צילומי לפני/אחרי של פרויקט אמיתי + אישור לקוח | 3.1 |
| 5 | קופי: מילת הענק, נוסח מד הטעינה, כותרת פס ההוכחות | 2.x/3.4 |
| 6 | לוגואי לקוחות/טכנולוגיות ל-Marquee (טכנולוגיות לא דורש אישור) | 1.4 |

---

## 9. מקורות מפתח מהמחקר
- טרנדים: Fontfabric/Envato/Creative Bloq 2026 — kinetic type, גריין, restraint doctrine
- עברית: AlefAlefAlef trends-2026 (אותיות דקיקות, מולטילינגואל הומוגני, מונו), פונטימונים, One Zero/gwspirits/mdb.studio כבנצ'מרקים
- ביצועים: motion.dev tier-list (S-tier: transform/opacity/clip-path/filter), MDN scroll-driven animations, Josh Comeau
- אמון: scalify 2026 (סיגנל אמון אחד ליד כל CTA), עקרון "claim → live verification → dare"
- ⛔ נדחה במחקר: WebGL hero (הורג מובייל), feTurbulence חי, פיצול אותיות עברי, scroll-jacking
