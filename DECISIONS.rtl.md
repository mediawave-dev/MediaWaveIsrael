# DECISIONS — ריצת design/wave-lab (03/07/2026)

החלטות פתוחות שהוכרעו אוטונומית לפי חוזה האוטונומיה (האופציה השמרנית + תיעוד).

## Phase 1

| # | החלטה | נימוק |
|---|---|---|
| 1 | **גריין כקובץ `public/images/grain.png` (6KB) ולא base64 ב-CSS** | אותו אפקט בדיוק; קובץ נפרד לא מנפח את ה-bundle הקריטי, נטען במקביל ונשמר בקאש. רעש מכומת ל-5 רמות סביב אפור-אמצע (נייטרלי ל-blend-mode overlay) כדי שה-PNG יידחס טוב. |
| 2 | **גריין ב-z-index 1** (מתחת להדר ולווידג'טים הצפים) | לפי התוכנית. ההדר הקבוע והמודאלים נשארים נקיים. |
| 3 | **`--font-spec` = מונו מערכתי + tabular-nums, עברית נופלת ל-Heebo** | לפי מדיניות הפונטים בפרומפט — בלי רכישה, בלי פונט חדש. רישיון EFT נשאר פתוח. |
| 4 | **מיקום הגלים: בתוך הסקשנים (absolute) בגבולות עם רקע תמונה/גרדיאנט, ובזרימה (in-flow) בגבולות עם צבעים אחידים** | כך אין תפרים: הגל תמיד נצבע בצבע האחיד של הסקשן הסמוך מעל רקע "חי". |
| 5 | **צבע הגל WhyUs→Services = `#F5F0E8`** (צבע ה-overlay התחתון של WhyUs) | ה-overlay של WhyUs הוא 94% אטום — הסטייה בלתי נראית. |
| 6 | **שכבות drift: בהירו ובכניסה לפוטר** (2 בדיוק כמו בתוכנית) | הגבולות הדרמטיים ביותר (כהה↔בהיר) — שם העומק משתלם. מושהה מחוץ למסך + כבוי ב-reduced-motion. |
| 7 | **Marquee: שמות טכנולוגיות בלבד** (React, TypeScript, Vite, Tailwind, Framer Motion, Cloudflare Pages, Node.js, Core Web Vitals) | לא דורש אישור קופי לפי התוכנית (סעיף 8.6). בלי לוגואי לקוחות — אין נכסים מאושרים. |
| 8 | **צל hover גם ב-HowWeWork** (לא רק Services/WhyUs) | אותה בעיית ביצועים בדיוק (transition-all על box-shadow); תוקן באותה טכניקה. |

## Phase 2

| # | החלטה | נימוק |
|---|---|---|
| 9 | **מילות מפתח בכותרות: `#0284C7` (sky-600) על רקעים בהירים, `#7DD3FC` על כהים** | ‎#7DD3FC על קרם = ניגודיות ‎~1.5:1 — נכשל WCAG גם לטקסט ענק. sky-600 שומר את זהות התכלת ועובר 3:1. על הרקע הכהה של ProofBand — ‎#7DD3FC המקורי. |
| 10 | **ProofBand: רק מספרים הניתנים לאימות מהריפו** — ציון Lighthouse נמדד, LCP נמדד, מספר עמודים, משקל דף | לפי מדיניות הפרומפט. סלוטים "פרויקטים/שנים" בנויים אך מוסתרים (`data-pending`). |
